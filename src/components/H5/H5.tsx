import { H5Props } from '@bluebase/components';
import React from 'react';
import Typography from '@material-ui/core/Typography';

export const H5 = (props: H5Props) =>
	<Typography style={{} as any} variant="h5" {...props}/>;
