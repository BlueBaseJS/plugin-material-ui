import { H3Props } from '@bluebase/components';
import React from 'react';
import Typography from '@material-ui/core/Typography';

export const H3 = (props: H3Props) =>
	<Typography style={{} as any} variant="h3" {...props}/>;