import { Body1Props } from '@bluebase/components';
import React from 'react';
import Typography from '@material-ui/core/Typography';

export const Body1 = (props: Body1Props) =>
	<Typography style={{} as any} variant="body1" {...props}/>;
